import styled from 'styled-components';

export const TagInputWrap = styled.div`
  position: relative;
  margin: 8px;
`;

export const TagInputForm = styled.form`
  position: relative;
  display: flex;
  width: 100%;
  padding-top: 16px;

  & > ul {
    width: 100%;
  }
`;
